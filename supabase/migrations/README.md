# Supabase Migrations

SQL files here are applied manually via the Supabase SQL Editor:
https://supabase.com/dashboard/project/gevdecxvpvopvdjjpaum/sql/new

Files are named by date. Apply in order.

## Status

| File | Applied to live? |
| --- | --- |
| `20260601023647_create_observation_requests.sql` | Yes |
| `20260616_phase2_membership.sql` | Yes |
| `20260623_waivers_reshape_guardian_usafnum.sql` | Yes |
| `20260629_photo_release.sql` | Yes |
| `20260630_family_accounts.sql` | Yes |
| `20260630_usaf_report_tracking.sql` | Yes |
| `20260703_usaf_citizenship_fields.sql` | Yes |
| `20260704_membership_hardening.sql` | Yes — applied 2026-07-04 via the Supabase MCP (all four parts; Part 2 had previously been applied by hand). Recorded in the remote migration ledger. |
| `20260711_signup_invite_tracking.sql` | Yes — applied 2026-07-11 via the Supabase MCP (`apply_migration`). Recorded in the remote ledger as `20260711160559_signup_invite_tracking`. |
| `20260727_volunteer_foundations.sql` | Yes — applied 2026-07-29 via the Supabase MCP (`apply_migration`) after an independent review pass. Recorded in the remote ledger as `volunteer_foundations`. Verified post-apply (see the file's STATUS header). **Note:** Part 3's `auth.users` trigger uses a `pg_trigger`-guarded conditional `CREATE` rather than this folder's usual `DROP ... IF EXISTS` / `CREATE` pattern — `DROP TRIGGER` needs ownership of `auth.users`, which `postgres` does not have. Do not normalize it. |
| `20260820_volunteer_staff_crud.sql` | Yes — applied 2026-08-20 via the Supabase MCP (`apply_migration`, recorded in the remote ledger as `volunteer_staff_crud`). M2 of the volunteer system: `events` + `volunteer_slots` tables, their RLS policies, an `events_updated_at` trigger (reusing `set_updated_at()` from `20260616_phase2_membership.sql`), and the two `REVOKE`s owed from M1. Verified post-apply: `profiles`/`account_settings` row counts unchanged (57/337); both new tables created empty; 5 policies on `events` and 4 on `volunteer_slots` (no missing `DELETE` policy); both `REVOKE`s confirmed via `has_function_privilege()` (not just the statement's exit status — see the lesson logged below); `has_role_at_least` still callable as required. `get_advisors` shows no findings beyond the pre-existing baseline and the already-accepted "multiple permissive policies" pattern `account_settings` already carries from M1. |
| `20260820_volunteer_signups.sql` | Yes — applied 2026-08-23 via the Supabase MCP (`apply_migration`, recorded in the remote ledger as `volunteer_signups`). M3 of the volunteer system: `volunteer_signups` table, its two RLS SELECT policies, the `claim_volunteer_slot`/`cancel_volunteer_signup` RPCs, and `slot_fill_counts()`. Verified post-apply: `profiles`/`account_settings`/`events`/`volunteer_slots` row counts unchanged (57/365/0/0, elevated count 1); `volunteer_signups` created empty; all 11 policies present across `events`/`volunteer_slots`/`volunteer_signups`. **Post-apply verification caught that `anon` could execute all three new `SECURITY DEFINER` functions despite their `REVOKE ALL ... FROM PUBLIC` statements** — see `20260823_volunteer_revoke_anon_function_execute.sql` and the corrected lesson below; this is not a flaw specific to this file; it's a project-wide gap in every prior migration's `REVOKE` pattern that only became visible here because it was the first time post-apply verification specifically checked `anon` (not just `authenticated`) against a *newly written* function. **Retro-synced 2026-09-01 (documentation pass) — the repo copy of this file was edited even though it is applied, which this folder normally forbids.** The exception was made because the file was *actively misleading*, not because live changed: its policies carried no `TO` clause and its `REVOKE`s named only `PUBLIC`, so anyone reading or replaying it would have concluded live was more open than it is, and a replay would have re-opened both gaps. The file now carries the `TO authenticated` clauses and the `anon`-naming `REVOKE`s that live actually has (folding in `20260823_volunteer_events_restrict_anon.sql`'s pattern and `20260823_volunteer_revoke_anon_function_execute.sql`'s revokes). **No live change was involved — nothing was applied.** Syncing it makes it honest, not replayable; it still must not be re-run. Note also that the text actually applied to live (ledger `20260823231015`) already contained `TO authenticated` on both `volunteer_signups` policies — the repo copy had been an older draft all along, which is why the 08-23 events fix does not mention this table. |
| `20260823_volunteer_events_restrict_anon.sql` | Yes — applied 2026-08-23 via the Supabase MCP (`apply_migration`, recorded in the remote ledger as `volunteer_events_restrict_anon`). Re-creates all 5 `events` and all 4 `volunteer_slots` policies (live since M2) with `TO authenticated` added — the original M2 migration wrote every policy with no `TO` clause, which in Postgres defaults to `PUBLIC` (every role, including `anon`). The 4 coach-gated policies per table were safe regardless (`has_role_at_least('coach')` is false for an anonymous request), but the two "published = true" SELECT policies had no role check at all, so published events/slots were genuinely readable by anon over `/rest/v1`. No table/column/data change. Verified post-apply: all 9 policies now show `roles = {authenticated}` in `pg_policies`. |
| `20260823_volunteer_revoke_anon_function_execute.sql` | Yes — applied 2026-08-23 via the Supabase MCP (`apply_migration`, recorded in the remote ledger as `volunteer_revoke_anon_function_execute`), immediately after `20260820_volunteer_signups.sql` in the same session, as a same-day correction to what that file's own `REVOKE` statements failed to do. See "REVOKE FROM PUBLIC does not work on this project" below — this is the actually-important lesson superseding the one previously recorded here. |
| `20260829_roles_and_nonathlete_profiles.sql` | Yes — applied 2026-08-29 via the Supabase MCP (`apply_migration`, recorded in the remote ledger as `roles_and_nonathlete_profiles`). M3.5 of the volunteer system, in five parts: `account_settings.role_updated_at`/`role_updated_by` (the FK is `ON DELETE SET NULL`, deliberately — `CASCADE` would delete the settings row, and therefore the role, of every account a departing admin ever granted); `profiles_person_type_check` widened to accept `'volunteer'`; a new `profiles_person_type_guard` trigger (the column-immutability guard M1's review deferred until "guardian rows become reachable"); a hardened `prevent_self_role_change()` that also refuses `admin` grants and self-changes and stamps the attribution columns; and `admin_account_list()`, an admin-only `SECURITY DEFINER` RPC feeding the role picker. Verified post-apply in full — see the file's STATUS header, which records the grant matrix per role, unchanged row counts (478/478/58/58/1), and **six behavioural tests executed inside a rolled-back transaction** impersonating a member and then the admin via `request.jwt.claims`. **The most important of those is test 1:** an ordinary member self-update of `volunteer_last_seen_at` still succeeds. Part 4 rewrote a trigger that fires on every member's own `account_settings` row on every visit to `/member/volunteer` (`markVolunteerSeen()`), and that call discards its own errors — so hoisting any of the new clauses above the `NEW.role IS DISTINCT FROM OLD.role` guard would have broken the feature's busiest page silently, for all 478 accounts. Anything editing this function again must re-run that test from a real session; the SQL editor passes it vacuously, because `auth.uid()` is NULL there and the entire guarded block is skipped. |
| `20260829_volunteer_staff_roster.sql` | Yes — applied 2026-08-29 via the Supabase MCP (`apply_migration`, recorded in the remote ledger as `volunteer_staff_roster`). A first, partial slice of M4: adds `event_roster()`, a coach+-only `SECURITY DEFINER` RPC that resolves attendee name/phone/notes for an event's live signups — needed because `profiles` has no coach-level read policy (only "Owners manage their members"), so a coach can see every `volunteer_signups` row directly but can't resolve `attendee_profile_id` to a name for a household other than their own without it. Written for `src/app/member/staff/events/[id]/roster/page.tsx`. `REVOKE` follows the corrected, anon-naming pattern from the start (no `FROM PUBLIC`-only mistake to repeat) — verified post-apply via `has_function_privilege()`: `anon_can_execute = false`, `authenticated_can_execute = true`. `get_advisors` shows no new findings beyond the accepted baseline (the expected `authenticated_security_definer_function_executable` note, same shape as the other three volunteer RPCs; no `anon` warning). No `attended`/`credited_hours`/hours-view work — that remains the rest of M4. |
| `20260830_roles_hardening_followup.sql` | Yes — applied 2026-08-30 via the Supabase MCP (`apply_migration`, recorded in the remote ledger as `roles_hardening_followup`). Three corrections to the previous day's M3.5 migration, found by an independent validation pass. **(1)** The role-guard trigger *wrote* `role_updated_at`/`role_updated_by` on a role change but never *protected* them otherwise — and `account_settings`' RLS grants a member UPDATE on their whole own row, so a member could PATCH a false "granted by" value onto themselves and have it render on `/member/staff/roles`. The 08-29 file's comment claiming this was "unforgeable by construction" was wrong; that comment is now annotated in place pointing here. Closed with an `ELSIF` that silently pins both columns back on any end-user update that does not change the role — **silently is mandatory**: raising instead would break `markVolunteerSeen()` for every account, which is the exact hazard the original Part 4 was written to avoid. **(2)** A partial unique index, `profiles_one_volunteer_per_account_idx ON profiles (account_owner_id) WHERE person_type = 'volunteer'`, because the app's check-then-insert had a gap a double-click could slip through — and a second row would permanently break that account's own `.maybeSingle()` lookup. **(3)** `admin_account_list` now escapes `%`, `_` and `\` in the search term and uses `ESCAPE '\'`, so a search for a literal underscore finds one and `%` no longer matches every account (never an injection risk — it was always a plpgsql expression, never dynamic SQL). Verified post-apply with a nine-point pass; see the file's STATUS header. **The two checks worth repeating on any future edit to this trigger are A and B: an ordinary member self-update must still succeed, and an attribution forgery must fail silently rather than raise.** |
| `20260831_staff_member_directory.sql` | Yes — applied via the Supabase MCP (`apply_migration`), recorded in the remote ledger as `staff_member_directory`, version `20260901014014` (i.e. late 2026-08-31 local / 2026-09-01 UTC). Adds one RPC, `staff_member_directory(text, text, text)`: a coach+-only `SECURITY DEFINER` lookup returning contact info, emergency contacts, class enrollment and medical notes for the staff member-directory screen. Same rationale as `event_roster()` — `profiles` has no coach-level read policy, so this is a narrow function rather than a blanket read grant. The return shape deliberately omits `sex_at_birth`, `gender_identity`, the USAF/citizenship columns and `member_waivers` entirely; do not widen it without a reason, since this function is the club's whole membership roster in one call. `REVOKE` follows the corrected anon-naming pattern from the start. **The file carries no STATUS header, so what its original post-apply pass actually checked is not recorded here and could not be reconstructed — do not assume the file's own "Post-apply verification" checklist was run.** What *was* re-verified live on 2026-09-01 during the migrations-folder documentation pass: `pg_proc.proacl` is `{postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}` and `has_function_privilege()` returns `anon = false`, `authenticated = true`. The role-gate behaviour test (a member-role session must get `not authorized`) and `get_advisors` were **not** re-run. |
| `20260831_volunteer_guardian_profile_rpc.sql` | **Yes — applied 2026-09-01** via the Supabase MCP (`apply_migration`, recorded in the remote ledger as `volunteer_guardian_profile_rpc`). Written 2026-08-31. Closes the `adults_only` bypass and the duplicate-guardian bug: a partial unique index on (account, normalized name) `WHERE person_type = 'guardian'` (deliberately not one-per-account — two parents are legitimate); `create_guardian_profile(uuid)`, a SECURITY DEFINER lookup-or-create that seeds a guardian row strictly from the caller's own athlete row's `guardian_*` columns, replacing the client-side INSERT in `claimSlot()`; a `BEFORE INSERT` guard on `profiles` (M3.5's `prevent_person_type_change()` is UPDATE-only, so `INSERT` was unguarded and a member could hand-craft a birthday-less `'guardian'` row and pass the `adults_only` check with it — verified live 2026-08-31 against `pg_policy`, `pg_constraint`, `pg_trigger` and the table grants); and a `claim_volunteer_slot` that tests adulthood positively per `person_type` instead of reading a NULL birthday as "adult". `'athlete'` and `'volunteer'` stay member-insertable (the enrollment wizard and D14's self-serve branch both write them from a member session); only `'guardian'` is closed, gated by a transaction-local GUC the RPC sets. **Not a security boundary and the file says so:** the free-text "Someone else…" path still accepts any name on an adults-only slot, by design. Post-apply, verify with `has_function_privilege()` for **anon and authenticated separately**, and re-run the two standing trigger checks from `20260830`: an ordinary member self-update must still succeed, and a real member signup must still work end to end (the new BEFORE INSERT trigger fires on the enrollment wizard's path too). **Applied and verified 2026-09-01:** `anon` cannot execute `create_guardian_profile` and neither `anon` nor `authenticated` can execute `prevent_person_type_insert`; index and trigger both present; row counts unchanged (59 profiles / 0 guardians / 516 settings). Six behavioural tests run inside a rolled-back transaction impersonating a real member: member self-update of `account_settings` still succeeds; a hand-crafted `guardian` INSERT is now blocked; a full `athlete` insert (the enrollment path) and a `volunteer` insert (D14) both still succeed; `create_guardian_profile()` succeeds for the caller's own row and is refused for a stranger's. Live `claim_volunteer_slot` was diffed against this file before applying — no drift, nothing silently reverted. |
| `20260901_policy_roles_authenticated.sql` | **Yes — applied 2026-09-01** via the Supabase MCP (`apply_migration`, recorded in the remote ledger as `policy_roles_authenticated`). Adds `TO authenticated` to the five remaining `roles = {public}` policies in this feature's blast radius: all four on `account_settings` (from `20260727_volunteer_foundations.sql` Part 6) and `"Owners manage their members"` on `profiles` (from `20260630_family_accounts.sql`, last re-created by `20260704_membership_hardening.sql` Part 1 — **not** from the 07-27 file, contrary to what a first reading suggests). **Defense in depth, not an open hole:** unlike the two `published = true` policies that `20260823_volunteer_events_restrict_anon.sql` closed, every one of these five already gates on `auth.uid()` or `has_role_at_least()`, both NULL/false for an anonymous request — `anon` read nothing through them before this file. The value is that the role restriction stops depending on each predicate staying correct. Predicates reproduced from the live `pg_policies` catalog, not from the migration files (this feature has drifted once already); the **only** delta is the `TO` clause. `service_role` is unaffected (it holds `BYPASSRLS`), and `account_settings` has no INSERT policy at all, so signup — which runs through the definer trigger `handle_new_user_account_settings()` — never went through RLS here. Verified post-apply: all five show `roles = {authenticated}` in `pg_policies`, and **the standing regression check was re-run from an impersonated member session inside a rolled-back transaction** — a self-update of `volunteer_last_seen_at` still succeeds, which matters because `"Users update own account settings"` is one of the policies re-created here and `markVolunteerSeen()` discards its own errors. **Left out deliberately:** the three sibling `{public}` owner-scoped policies on `emergency_contacts`, `member_medical` and `member_waivers` (all `20260704` Part 1) — same shape, equally anon-unreachable, but they belong to the membership feature, not this one. Worth a follow-up file, otherwise "the schema is consistent now" is only true of two of five tables. `observation_requests` needs nothing (RLS on, zero policies, service-role-only writer). |
| `20260901_staff_directory_remove_medical.sql` | Yes — applied 2026-09-01 via the Supabase MCP (`apply_migration`, recorded in the remote ledger as `staff_directory_remove_medical`). Drops `medical_conditions`/`preferred_medical_system` from `staff_member_directory()`'s return shape — a coach-gated, club-wide roster call is too broad an audience for medical notes without a vetted secure channel, which doesn't exist yet. Intended to come back once a narrower channel is designed. **`CREATE OR REPLACE` failed live** (`cannot change return type of existing function` — Postgres refuses when the `RETURNS TABLE` column set changes), so the applied SQL is `DROP FUNCTION` + plain `CREATE FUNCTION`, which required reissuing the `REVOKE`/`GRANT` grants DROP wipes; the file was updated in place to match before this row was marked applied (permitted here only because it had never yet been applied — not a case of editing an already-applied file). Verified post-apply: `pg_get_function_result()` shows the new 21-column shape with no medical columns; `has_function_privilege()` returns `anon = false`, `authenticated = true`, matching `20260831`'s grants exactly. |
| `20260901_volunteer_staff_cancellation.sql` | **Yes — applied 2026-09-01** via the Supabase MCP (`apply_migration`, recorded in the remote ledger as `volunteer_staff_cancellation`). Staff-side cancellation, the soft-launch blocker: no screen let a coach remove a volunteer from a slot, while two staff error messages told them to do exactly that. Adds `volunteer_signups.cancelled_by` (FK to `auth.users`, **`ON DELETE SET NULL`** — `CASCADE` would delete the volunteer's own commitment record whenever a departing coach's login is removed, `RESTRICT` would make offboarding fail; same reasoning as `role_updated_by` in `20260829`) and `cancelled_reason` (plus a not-blank CHECK and a partial index), `account_settings.volunteer_cancellations_seen_at` for the unread badge, and two coach-gated `SECURITY DEFINER` RPCs: `staff_cancel_signup(uuid, text)` → boolean and `staff_cancel_slot_signups(uuid, text)` → integer. **`cancel_volunteer_signup` is deliberately NOT widened** — the member RPC keeps its own authorization model. **The app's staff-vs-member discriminator is `cancelled_reason IS NOT NULL`, never `cancelled_by IS NOT NULL`:** because the FK is SET NULL, a deleted coach account would otherwise silently reclassify a staff cancellation as a member's own and drop it from the volunteer's dashboard — the exact failure the feature exists to prevent. **Ground truth read live before writing:** `cancel_volunteer_signup` already contained `AND NOT public.has_role_at_least('coach')`, so a coach could always cancel any signup at the database; what live lacked was any record of who or why, which is what made a staff cancellation invisible. Verified post-apply: both RPCs `anon`-blocked / `authenticated`-allowed, all three columns present, and **eight behavioural tests run inside a rolled-back transaction** against a seeded event/slot/signup — both seen-at writes still succeed for an ordinary member (two `account_settings` columns are now written by error-discarding calls, not one); a member calling `staff_cancel_signup` is refused; a blank reason is refused; a coach cancel returns true, a second returns false without re-stamping, and bulk-cancel correctly reports 0 rows; attribution is stamped and not overwritten. **Known gap, not closed here:** `cancel_volunteer_signup`'s coach clause still permits a coach to cancel any signup with no reason and no attribution — unreachable from any UI, but a live hole in this file's guarantee. Closing it means dropping that clause in a follow-up migration now that a proper staff path exists. |

## Never re-run an applied file

- **If the table above says a file is applied, do not run it again.** Not to "make sure," not to reproduce live somewhere else.
- **Idempotent here means "won't crash on a second run," not "won't change anything."** Most files use `CREATE OR REPLACE FUNCTION` and `DROP POLICY IF EXISTS` / `CREATE POLICY`, which cheerfully overwrite a *newer* definition with the older one in the file. Re-running `20260727_volunteer_foundations.sql` today would roll `prevent_self_role_change()` back past two rounds of hardening; re-running `20260820_volunteer_staff_crud.sql` would strip `TO authenticated` off all 9 `events`/`volunteer_slots` policies again. Both would report success.
- **This folder is a chronological ledger, not a desired-state manifest.** It cannot be replayed from scratch to reproduce live, and no file here is guaranteed to describe the current definition of the objects it names — later files supersede parts of earlier ones. The database (`pg_policies`, `pg_get_functiondef`, `pg_proc.proacl`) is the only source of truth for what is live.
- **If live needs to change, write a new dated file.** Never edit one that has been applied.

## REVOKE FROM PUBLIC does not work on this project — always name the role

**Corrected 2026-08-23. The note this replaces had the lesson backwards; do not revert to it.**

This project's public-schema default privileges grant `EXECUTE` on every *newly created* function directly to `anon`, `authenticated`, and `service_role` — confirmed live via:

```sql
SELECT defaclrole::regrole, defaclobjtype, defaclacl
FROM pg_default_acl WHERE defaclnamespace = 'public'::regnamespace;
-- postgres | f | {postgres=X/postgres,anon=X/postgres,authenticated=X/postgres,service_role=X/postgres}
```

That grant is made **directly to each role**, not inherited through the `PUBLIC` pseudo-role. `REVOKE EXECUTE ON FUNCTION ... FROM PUBLIC` — the pattern used in every migration in this feature through 2026-08-23 — therefore does **nothing** to `anon`'s or `authenticated`'s ability to call a function. It only removes a grant that was never actually the one in effect.

This was discovered when `20260820_volunteer_signups.sql`'s three new `SECURITY DEFINER` functions were all still `anon`-executable immediately after their `REVOKE ALL ... FROM PUBLIC` statements ran — confirmed via `has_function_privilege('anon', <oid>, 'EXECUTE')` returning `true` and via `pg_proc.proacl` showing an explicit `anon=X/postgres` entry. Fixed same-day by `20260823_volunteer_revoke_anon_function_execute.sql`, which names the role directly:

```sql
REVOKE EXECUTE ON FUNCTION public.claim_volunteer_slot(uuid, uuid, text, uuid) FROM anon;
REVOKE EXECUTE ON FUNCTION public.cancel_volunteer_signup(uuid) FROM anon;
REVOKE EXECUTE ON FUNCTION public.slot_fill_counts(uuid) FROM anon;
```

**Going forward, every `REVOKE` on a function meant to be non-public must name `anon` (and `authenticated`, for a function no signed-in member should call directly, like a trigger function) explicitly — never `PUBLIC` alone.** `REVOKE ... FROM PUBLIC` is harmless to also include for defense-in-depth against a future project without this default-privilege quirk, but it cannot be the *only* statement.

### The `revoke_trigger_function_rpc` ledger entry was correctly diagnosed and does have effect — the opposite of what this section used to say

The remote ledger contains **`revoke_trigger_function_rpc`** (applied 2026-07-29) with no corresponding file here. The previous version of this note called it a no-op and told M2 to use `FROM PUBLIC` instead — that guidance was itself the bug, and it is exactly what let the M3 gap above happen.

Re-verified 2026-08-23: `handle_new_user_account_settings()` and `prevent_self_role_change()` (the two trigger functions this entry targeted) currently have **neither `anon` nor `authenticated`** in `pg_proc.proacl` — both roles are genuinely blocked. That is the ledger entry's `REVOKE EXECUTE ON FUNCTION ... FROM anon, authenticated` working exactly as intended; whatever check found it a "no-op" at the time was mistaken. The `REVOKE ... FROM PUBLIC` statements later added in M2's Part 5 for these same two functions were redundant — harmless, but they were not what actually closed the gap.

Do **not** revoke `public.has_role_at_least(account_role)` — it is referenced by RLS policies across every table in this feature, and a function used in a policy is evaluated as the *querying* role, so revoking it turns an ordinary member's `SELECT` into a permission error instead of an empty result. It is expected to remain `anon`- and `authenticated`-executable; `get_advisors` will flag this and it is an accepted, understood finding, not a bug.

Lesson worth keeping, still true: `REVOKE` reports success when it removes nothing. Always verify a permission change with `has_function_privilege()` for **every role that might hold the grant** — checking only `authenticated` (as every verification pass through M2 did) is exactly how the `anon` gap above went unnoticed for as long as it did.
